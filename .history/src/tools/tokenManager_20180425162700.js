import { AsyncStorage } from 'react-native';

 export function loggedIn(obj)  {
   

        if(Object.keys(value).length === 0){
            return false
        }else{
            return true
        }



}